import React from "react";
import styles from "./Planed.module.css";

const Planed = () => {
    return (
        <div className={`base_wrapper`}>
            <h1 className={`base_title`}>My planed</h1>
        </div>
    );
}

export default Planed;