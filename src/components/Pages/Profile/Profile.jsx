import React from "react";
import styles from "./Profile.module.css";

const Profile = () => {
    return (
        <div className={`base_wrapper`}>
            <h1 className={`base_title`}>My Profile</h1>
        </div>
    );
}

export default Profile;