import React from "react";
import styles from "./Settings.module.css";

const Settings = () => {
    return (
        <div className={`base_wrapper`}>
            <h1 className={`base_title`}>My Settings</h1>
        </div>
    );
}

export default Settings;