import React from "react";
import styles from "./Tasks.module.css";

const Tasks = () => {
    return (
        <div className={`base_wrapper`}>
            <h1 className={`base_title`}>My tasks</h1>
        </div>
    );
}

export default Tasks;